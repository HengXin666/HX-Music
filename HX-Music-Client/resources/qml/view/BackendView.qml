import QtQuick
import QtQuick.Controls
import QtQuick.Layouts
import QtQuick.Dialogs
import HX.Music
import "../widget/internal"

Item {
    id: root
    width: 500
    height: 400

    // 设置后端网址

    // 设置是否自动爬取歌词 (上传即爬取)
}